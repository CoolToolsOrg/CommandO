Setup:

  brew install --cask xbar (or download from xbarapp.com)
  Launch xbar — it'll ask you to choose a plugins folder (e.g. ~/xbar-plugins)    
  Drop the file in there, then in Terminal: chmod +x ~/xbar-plugins/quickcmds.1h.sh
  Click the ⚡ icon in your menu bar → Refresh All
